
const regex = /{}|()|\[\]|\<\>|/;

const puzzle1 = (data: string[]): number => {
    return 0;
}

const puzzle2 = (data: string[]): number => {
    return 0;
}

export {puzzle1, puzzle2};

